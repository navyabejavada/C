#include<stdio.h>
void main()
int a,b;
printf("Enter two nums");
scanf("%d%d,&a,&b");
int max=a>b?a:b;
int step=max;
while(2)
{
    if(max%a==0 && max%b==0)
    {
        printf("The lcm is %d",max);
        break;
    }
    else{
       max=max+step;
    }
}
